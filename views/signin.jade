extends layout
block header
	div.container
		div.navbar-header
			div.navbar-brand
block body
	div.panel.panel-primary
		div.panel-heading
			| Please sign in.
		div.panel-body(ng-controller='signin')
			form.form-signin(action='/users/signin', method='post')
				div.form-group
					label(for='username')
						| username
					input.form-control#username(type='text', name='username', placeholder='username', autofocus)
				div.form-group
					label(for='password')
						| password 
					input.form-control#password(type='password', name='password', placeholder='password')
				div.form-group
					button.btn.btn-lg.btn-primary.btn-block(type='submit')
						| Sign in
	scripts.
		function